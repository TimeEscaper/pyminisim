from typing import Tuple

import numpy as np


def circle_sector_intersection(circle: Tuple[float, float, float],
                               sector: Tuple[float, float, float]) -> bool:
    """

    :param circle: (x, y, radius)
    :param sector: (x, y, field-of-view (symmetric)
    :return: If has intersection or not
    """
    pass
